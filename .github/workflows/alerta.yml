name: Notificación CI a ntfy.sh

on:
  push:
    branches:
      - main

jobs:
  notify:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout del código
        uses: actions/checkout@v4

      - name: Ejecutar programa Hola Mundo
        run: node hola.js

      - name: Enviar notificación a ntfy.sh/devops-itla
        run: |
          curl -d " Nuevo código subido al repositorio $(basename $GITHUB_REPOSITORY)" \
               -H "Title: Alerta CI - Push detectado" \
               -H "Priority: 3" \
               https://ntfy.sh/devops-itla
